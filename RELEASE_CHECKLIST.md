# چک‌لیست تست گرم و تحویل آجر

این سند دروازه تحویل هر نسخه آجر به مشتری است. اجرای موفق تست‌ها به معنی سلامت فنی نسخه است؛ فعال‌سازی سرویس‌های بیرونی و کنترل بازیابی backup باید جداگانه در staging همان استقرار انجام شود.

## سناریوی گرم خودکار

ابتدا PostgreSQL و نسخه production برنامه را اجرا کنید:

```bash
npm run db:migrate
npm run build
PORT=3003 npm run start
```

در یک ترمینال دیگر:

```bash
npm run test:warm
```

تست یک دفتر موقت و ایزوله ایجاد می‌کند و این زنجیره را با همان داده بررسی می‌کند:

1. دفتر، مدیر، مشاور و پرونده پرسنلی
2. مالک، متقاضی و تقاضای خرید
3. فایل ملکی، تصویر خصوصی و پیگیری CRM
4. بازدید، بازخورد، وظیفه و پیشنهاد پذیرفته‌شده
5. معامله، تعرفه کمیسیون و سهم دفتر/مشاور
6. قرارداد، طرفین، چک‌لیست، نسخه امضاشده، شاهد، تعهد و پیوست خصوصی
7. وصول کمیسیون، گردش حسابداری، تسویه سهم مشاور و فروش نهایی ملک
8. رندر داشبورد، مخاطب، ملک، بازدید، پیشنهاد، معامله، حقوقی، چاپ قرارداد، کمیسیون، حسابداری و گزارش
9. خروجی CSV همان دفتر

تمام داده‌ها، نشست و فایل‌های تست در پایان—even در صورت خطا—پاک‌سازی می‌شوند.

## دروازه فنی پیش از انتشار

```bash
npm run typecheck
npm run lint
npm test
npm audit --omit=dev
npm run test:operational
npm run test:crm
npm run test:operations
npm run test:accounting
npm run test:legal
npm run test:security
npm run test:integrations
npm run test:warm
npm run build
```

- migrationها روی یک PostgreSQL خالی با `prisma migrate deploy` اجرا شوند.
- `/api/health/live` و `/api/health/ready` در Coolify پاسخ موفق بدهند.
- تست جداسازی دو دفتر و فایل خصوصی موفق باشد.
- هیچ خطای TypeScript، lint، unit test، build یا مرورگر باقی نماند.
- seed نمایشی روی دیتابیس مشتری اجرا نشود؛ فقط bootstrap اجرا شود.
- حساب‌ها و رمزهای seed حتی با تنظیم اشتباه `SEED_DEMO_DATA` در build تولیدی نمایش داده نشوند.

## آزمون staging با سرویس واقعی

این موارد به کلید و حساب واقعی وابسته‌اند و نباید روی محیط توسعه یا بدون تأیید مالک سرویس اجرا شوند:

- یک پرداخت کم‌مبلغ در Sandbox زرین‌پال: ایجاد Authority، بازگشت callback، ثبت `refId` و فعال‌شدن فقط یک اشتراک.
- یک پیامک قالبی SMS.ir به شماره مجاز: ثبت شناسه پیام، وضعیت `SENT` و رخداد موفق.
- یک جست‌وجوی AI با عبارت شامل شهر و محله: عدم خروج اطلاعات دفتر دیگر و ثبت latency.
- قطع موقت هر سرویس و مشاهده پیام خطای فارسی، retry محدود و رخداد خطا در سوپرادمین.
- اجرای Cron با `CRON_SECRET` و کنترل بازیابی یک پیامک رهاشده.

## کنترل استقرار Coolify

- دامنه و SSL فعال و `APP_URL` دقیقاً برابر دامنه عمومی باشد.
- رمز PostgreSQL، `SESSION_SECRET`، کلیدهای Server Action و `SETTINGS_ENCRYPTION_KEY` قوی و یکتا باشند.
- حساب سوپرادمین فقط از bootstrap ساخته و رمز پیش‌فرض فوراً تعویض شود.
- Volumeهای PostgreSQL، upload و backup دائمی باشند.
- برنامه backup سبز باشد و restore ماهانه روی staging جدا آزمایش شود.
- retention، ظرفیت دیسک، healthcheck و هشدار خطای سرویس‌ها فعال باشند.
- شرایط استفاده، حریم خصوصی و متن قراردادها توسط مشاور حقوقی ایران بازبینی شوند.

## پذیرش مشتری

- مدیر دفتر با حساب واقعی خود وارد شود و یک مشاور بسازد.
- یک فایل فروش و یک فایل اجاره، یک مالک و یک متقاضی ثبت شوند.
- دسترسی مشاور محدود و جداسازی اطلاعات دفتر تأیید شود.
- خروجی CSV، چاپ قرارداد شمسی، کمیسیون و گزارش مالی با ارقام مورد توافق دفتر تطبیق داده شوند.
- مسئول backup، سوپرادمین، پشتیبانی و تمدید اشتراک مشخص باشد.
- نتیجه پذیرش، نسخه استقرار و تاریخ شمسی تحویل ثبت شود.
