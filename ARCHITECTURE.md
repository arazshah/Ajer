# معماری آجر

## مرزهای امنیتی

`Agency` مرز tenant است. تمام داده‌های تجاری دارای `agencyId` هستند و تمام query و mutationهای کاربران عادی با `agencyId` نشست محدود می‌شوند. شناسه‌های دریافتی فرم نیز پیش از ساخت رابطه، دوباره در همان tenant بررسی می‌شوند. سوپرادمین در جدول و cookie مجزا قرار دارد و هیچ مسیر ثبت‌نام عمومی برای آن وجود ندارد.

نشست‌ها token تصادفی ۲۵۶ بیتی دارند و فقط hash آن در `AuthSession` ذخیره می‌شود؛ بنابراین نشست هر کاربر، دفتر یا سوپرادمین فوراً قابل ابطال است. cookieها `HttpOnly`، در تولید `Secure` و برای مدیریت کل `SameSite=Strict` هستند. ورود ناموفق هم بر اساس هویت و IP در دیتابیس محدود می‌شود و پس از پنج خطا، حساب و مبدأ برای ۱۵ دقیقه قفل می‌شوند. موفقیت، شکست، خروج، تعلیق دفتر، ابطال نشست و تغییر تنظیمات حساس در `SecurityEvent` ثبت می‌شوند.

هدرهای CSP، HSTS در تولید، منع frame، جلوگیری از MIME sniffing و Permissions Policy در سطح همه مسیرها اعمال می‌شوند. Server Action و Route Handler همچنان داخل خود عملیات احراز هویت، مجوز و مالکیت رکورد را دوباره بررسی می‌کنند.

کلیدهای AI، زرین‌پال و SMS.ir فقط در Route Handler یا Server Action استفاده می‌شوند. مرورگر هرگز merchant ID خصوصی، کلید پیامک یا کلید AI را دریافت نمی‌کند.

## عضویت و دسترسی

ثبت‌نام یک `Agency` و اولین `User` با نقش `ADMIN` را در یک transaction می‌سازد. دوره آزمایشی ۳۰ روز است و AI را نیز شامل می‌شود. پس از پایان دوره، `requireUser` مسیرهای عملیاتی را به صورتحساب هدایت می‌کند؛ صفحه صورتحساب و راهنما همچنان برای تمدید قابل دسترسی‌اند.

پلن‌ها در دیتابیس قرار دارند. درخواست پرداخت ابتدا یک `Payment` تغییرناپذیر از نظر مبلغ و مدت می‌سازد. callback زرین‌پال authority و مبلغ همان رکورد را verify و سپس در transaction اشتراک را فعال می‌کند. callback idempotent است و پرداخت تأییدشده دوباره اشتراک نمی‌سازد.

## داده و اجرا

PostgreSQL دیتابیس اصلی است و migrationها با `prisma migrate deploy` اجرا می‌شوند. Compose برای PostgreSQL، برنامه و backup healthcheck مستقل دارد. readiness علاوه بر دیتابیس، فضای فایل و تازگی backup را کنترل می‌کند. سرویس backup با `pg_dump`، آرشیو upload و checksum نسخه‌های زمان‌دار می‌سازد و restore فقط با پرچم صریح و پس از توقف ترافیک ممکن است. اسکریپت bootstrap پلن‌های پیش‌فرض و حساب سوپرادمین محیط را بدون بازنویسی تنظیمات موجود ایجاد می‌کند.

`FileAsset` فقط metadata را در PostgreSQL نگه می‌دارد. payload با abstraction مشترک روی volume خصوصی یا S3-compatible private قرار می‌گیرد؛ دانلود در هر دو حالت از Route Handler احراز هویت‌شده عبور می‌کند تا tenant و سطح رکورد بررسی شود.

جست‌وجوی AI فقط متن شرایط را برای استخراج معیارهای ساخت‌یافته ارسال می‌کند. رکوردهای مالک و متقاضی به سرویس AI فرستاده نمی‌شوند و فیلتر و امتیازدهی نهایی داخل دیتابیس خصوصی همان دفتر انجام می‌شود.

## یکپارچه‌سازی‌های بیرونی

تمام ارتباط‌های AI، SMS.ir و زرین‌پال در سرور timeout، سقف اندازه پاسخ، اعتبارسنجی JSON و شناسه درخواست دارند. retry فقط برای خطاهای گذرای شبکه و سرویس انجام می‌شود؛ درخواست ساخت پرداخت و ارسال پیامک در همان پردازش تکرار نمی‌شوند تا پرداخت یا پیام تکراری ساخته نشود. verify پرداخت که idempotent است retry محدود دارد و callback با قفل تراکنشی PostgreSQL از فعال‌سازی هم‌زمان دو اشتراک جلوگیری می‌کند.

پیامک‌های عملیاتی ابتدا در `SmsDispatch` ثبت و سپس با claim اتمیک پردازش می‌شوند. پردازش رهاشده پس از ده دقیقه بازیابی و تلاش‌های بعدی با فاصله ۵، ۲۰ و ۶۰ دقیقه زمان‌بندی می‌شوند. نتیجه سرویس‌ها بدون ذخیره کلید یا متن حساس در `IntegrationEvent` ثبت می‌شود و سوپرادمین نرخ موفقیت، خطا، latency و تعداد تلاش را می‌بیند؛ رخدادهای این بخش پس از ۱۸۰ روز توسط Cron پاک می‌شوند.

## دفتر مالی

هر دفتر حساب‌های صندوق، بانک و تنخواه و دسته‌بندی‌های درآمد/هزینه مستقل دارد. تمام مبالغ با `BigInt` و واحد تومان ذخیره می‌شوند. درآمد و هزینه، مطالبات و بدهی‌ها مبنای سود و زیان تعهدی‌اند و دریافت، پرداخت و انتقال فقط مانده حساب‌ها را تغییر می‌دهند تا تسویه دوباره در سود محاسبه نشود.

وصول معامله، تسویه پورسانت پرسنل، چک و حقوق به `FinanceTransaction` متصل‌اند. ابطال عملیات تسویه‌شده یا دارای چک وصولی و حقوق پرداخت‌شده مسدود است و تمام ارجاعات مالی پیش از ثبت با `agencyId` دفتر کنترل می‌شوند.

## تاریخ و گزارش

تمام تاریخ‌های رابط با تقویم شمسی نمایش داده می‌شوند. ورودی‌های تاریخ نیز رشته شمسی را می‌پذیرند، در سرور اعتبارسنجی می‌شوند و با منطقه زمانی `Asia/Tehran` به زمان استاندارد دیتابیس تبدیل می‌گردند؛ بنابراین تنظیم منطقه زمانی مرورگر یا سرور تاریخ عملیات دفتر را جابه‌جا نمی‌کند.

گزارش مدیریتی تمام queryها را با `agencyId` و بازه انتخابی محدود می‌کند. قیف فروش بر اساس متقاضی یکتا محاسبه می‌شود و گزارش مالی تنها برای دارنده مجوز حسابداری، با منطق سود و زیان تعهدی مشترک دفتر مالی نمایش داده می‌شود.

## پرونده حقوقی

`DealContract` ریشه پرونده حقوقی هر معامله است. اطلاعات طرفین در `ContractParty` به‌صورت snapshot ذخیره می‌شوند تا ویرایش بعدی CRM متن تاریخی قرارداد را تغییر ندهد. متن قرارداد هرگز روی نسخه موجود بازنویسی نمی‌شود و هر بازنگری یک `ContractVersion` جدید با سازنده، زمان و خلاصه تغییرات می‌سازد.

گردش نسخه به‌ترتیب پیش‌نویس، بازبینی، نهایی و امضاشده کنترل می‌شود. ثبت امضا تنها با شماره و تاریخ قرارداد، حداقل دو طرف اصلی امضاشده و تأیید تمام موارد الزامی چک‌لیست ممکن است. پیوست‌ها از `FileAsset` خصوصی دفتر استفاده می‌کنند و دانلود آن‌ها علاوه بر tenant، با دسترسی همان معامله کنترل می‌شود.
