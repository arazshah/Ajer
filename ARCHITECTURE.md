# معماری آجر

آجر یک برنامه یکپارچه Next.js با App Router است. صفحات خواندنی به‌صورت Server Component مستقیماً از Prisma داده می‌گیرند و همه تغییرات از Server Actionهای اعتبارسنجی‌شده عبور می‌کنند.

## مرز ماژول‌ها

- `app/(dashboard)`: صفحات محافظت‌شده عملیاتی
- `app/actions.ts`: نقطه ورود تغییرات، کنترل نشست و مجوز
- `components`: پوسته RTL، فرم‌ها، نمودارها و نقشه‌های client-only
- `lib/db.ts`: نمونه singleton کلاینت Prisma
- `lib/auth.ts`: نشست JWT در کوکی HTTP-only با انقضای ۸ ساعته
- `lib/matching.ts`: موتور امتیازدهی قاعده‌محور و قابل توضیح
- `lib/ai-property-search.ts`: schema تفسیر زبان طبیعی و امتیازدهی امن فایل‌ها
- `lib/format.ts`: پول، ارقام، موبایل، تاریخ و BigInt
- `prisma/schema.prisma`: مدل رابطه‌ای و ایندکس‌ها

SQLite برای نسخه نمایشی محلی انتخاب شده است. مقادیر پولی `BigInt` هستند و پیش از عبور به Client Component سریال می‌شوند. نقشه فقط در مرورگر بارگذاری می‌شود تا وابستگی Leaflet به `window` باعث خطای SSR نشود. کنترل نقش مدیر در سرور انجام می‌شود.

در تولید پیشنهاد می‌شود SQLite با PostgreSQL/PostGIS، فایل محلی با object storage، JWT محلی با نشست قابل ابطال و کاشی عمومی OSM با سرویس دارای سیاست مصرف مناسب جایگزین شود.

## جست‌وجوی هوشمند

Route Handler سرور متن فارسی را به endpoint سازگار OpenAI در AvalAI ارسال می‌کند. مدل فقط معیارهای ساخت‌یافته JSON تولید می‌کند و هیچ رکورد مالک یا متقاضی دریافت نمی‌کند. اعتبارسنجی پاسخ با Zod و اعمال فیلتر روی SQLite انجام می‌شود؛ بنابراین تصمیم نهایی قابل کنترل و کلید API خارج از bundle مرورگر است.
