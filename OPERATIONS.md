# راهنمای عملیات و امنیت آجر

## کنترل روزانه

- وضعیت PostgreSQL، برنامه و سرویس backup در Coolify سبز باشد.
- `/api/health/ready` پاسخ ۲۰۰ بدهد و آخرین backup کمتر از ۳۰ ساعت عمر داشته باشد.
- مرکز امنیت سوپرادمین برای ورودهای ناموفق غیرعادی، حساب‌های قفل و نشست‌های فعال بررسی شود.
- خطاهای صف پیامک و callback پرداخت مرور شوند.
- در بخش «یکپارچه‌سازی‌ها» پنل سوپرادمین، افت نرخ موفقیت یا رشد latency سرویس‌های AI، SMS.ir و زرین‌پال بررسی شود.

## کنترل هفتگی

- یک نسخه backup به فضای خارج از سرور Coolify منتقل و checksum آن کنترل شود.
- مصرف دیسک دیتابیس، upload و backup بررسی شود.
- کاربران غیرفعال، سطح دسترسی مالی/خروجی و نشست دفاتر خاتمه‌یافته بازبینی شوند.
- به‌روزرسانی‌های امنیتی Node، Next.js، Prisma و تصویر PostgreSQL ابتدا در staging آزموده شوند.

## آزمون بازیابی ماهانه

1. یک محیط staging جدا با PostgreSQL خالی بسازید.
2. dump و آرشیو فایل یک timestamp یکسان را با `restore-backup` بازیابی کنید.
3. migration، `npm run test:security`، `npm run test:integrations` و تست‌های گرم حوزه‌های عملیاتی را اجرا کنید.
4. ورود، دانلود یک فایل خصوصی، قرارداد چاپی، مانده حساب و گزارش را با نمونه backup مقایسه کنید.
5. زمان بازیابی واقعی را ثبت کنید؛ هدف اولیه RPO برابر ۲۴ ساعت و RTO کمتر از ۴ ساعت است.

## پیش از تحویل نسخه

- تمام دستورات بخش «دروازه فنی» در `RELEASE_CHECKLIST.md` اجرا شوند.
- `npm run test:warm` باید همان پرونده را از CRM تا قرارداد، مالی و گزارش عبور دهد.
- آزمون‌های واقعی AI، SMS.ir و Sandbox زرین‌پال فقط در staging و با کلیدهای همان محیط انجام شوند.
- نسخه commit، migration اعمال‌شده، تاریخ شمسی آزمون و نتیجه پذیرش مشتری ثبت شوند.

## واکنش به رخداد

1. در صورت سرقت حساب یک دفتر، از مرکز امنیت سوپرادمین همه نشست‌های همان دفتر را باطل و در صورت نیاز دفتر را تعلیق کنید.
2. رمز حساب درگیر و هر کلید سرویس افشاشده را تعویض کنید. تغییر `SETTINGS_ENCRYPTION_KEY` بدون برنامه مهاجرت ممنوع است.
3. رویدادهای امنیتی، AuditLog، IP، زمان شمسی رخداد و دامنه داده‌های درگیر را حفظ کنید.
4. قبل از پاک‌سازی، snapshot فقط‌خواندنی بگیرید؛ سپس علت رخداد را برطرف و دسترسی را مرحله‌ای باز کنید.
5. در رخداد پایگاه داده، روی سرور اصلی آزمایش restore انجام ندهید؛ ابتدا محیط جدا را بازیابی و صحت را کنترل کنید.

## سیاست کلید و دسترسی

- اسرار فقط در Environment Variables خصوصی Coolify یا تنظیمات رمزنگاری‌شده سوپرادمین نگهداری شوند.
- هیچ کلید محرمانه‌ای با `NEXT_PUBLIC_` تعریف نشود.
- دسترسی مالی، خروجی داده، مدیریت کاربران و تنظیمات فقط به افراد مسئول داده شود.
- حساب سوپرادمین عمومی نیست، نشست چهار ساعته دارد و بعد از پنج ورود ناموفق ۱۵ دقیقه قفل می‌شود.
- نشست کاربران هشت ساعته، `HttpOnly`، `Secure` در تولید و قابل ابطال مرکزی است.

## خرابی سرویس بیرونی

- برای AI، پاسخ‌های 429 و 5xx و خطای شبکه حداکثر دو بار و فقط در محدوده همان درخواست تکرار می‌شوند؛ timeout برابر ۲۵ ثانیه است.
- برای پیامک، retry فوری انجام نمی‌شود. صف پایدار تلاش بعدی را زمان‌بندی می‌کند تا در صورت گم‌شدن پاسخ، احتمال پیام تکراری کاهش یابد.
- ساخت پرداخت زرین‌پال retry فوری ندارد. verify تا دو تلاش محدود است و کدهای 100 و 101 موفق تلقی می‌شوند.
- پیش از تغییر کلید یا endpoint، آخرین `IntegrationEvent`ها، ساعت رخداد و پاسخ health بررسی شوند. کلیدها یا پاسخ خام سرویس را در log کپی نکنید.
